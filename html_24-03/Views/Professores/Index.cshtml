@model IEnumerable<html_24_03.Models.Professores>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ProfessoresNome)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ProfessoresCurso)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ProfessoresNome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ProfessoresCurso.cursosId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ProfessoresId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ProfessoresId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ProfessoresId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
