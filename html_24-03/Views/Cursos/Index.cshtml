@model IEnumerable<html_24_03.Models.Cursos>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.cursoNome)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.cursoNome)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.cursosId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.cursosId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.cursosId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
